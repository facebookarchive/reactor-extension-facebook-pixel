/* eslint-disable camelcase */
export default ({
  value,
  currency,
  content_category,
  content_ids,
  content_name,
  contents,
  num_items,
  status
}) => ({
  value,
  currency,
  content_category,
  content_ids,
  content_name,
  contents,
  num_items,
  status
});
